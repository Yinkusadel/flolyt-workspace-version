/**
 * Placeholder for screens 047-049 (home consumer / accounts / hybrid) in
 * files (24)/flolyt-spec.md — layout shell only until those are built.
 * See docs/build-tracker.md.
 */
const Home = () => {
  return (
    <div className="rounded-card border border-line bg-paper p-6 text-ink-3">
      Home
    </div>
  );
};

export default Home;
