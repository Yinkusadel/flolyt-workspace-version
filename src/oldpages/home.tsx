/**
 * Placeholder for screens 11-12 (workspace home consumer / accounts) in
 * flolyt-kit-122 — layout shell only until those are built. See
 * docs/build-tracker.md.
 *
 * Archived, not routed — the sidebar's "viewing as" scope selector this used
 * to echo was removed from the app shell in the rooms-first redesign.
 */
const Home = () => {
  return <div className="rounded-card border border-line bg-paper p-6 text-ink-3">Home</div>;
};

export default Home;
